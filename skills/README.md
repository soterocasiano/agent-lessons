# Skills

This directory contains agent skill definitions and implementations.

Each skill describes a capability that an agent can learn or exercise, along with any required context, inputs, and expected outputs.

## Adding a Skill

Create a new file named after the skill (e.g., `skill-<name>.md` or an appropriate code file) and document its purpose, parameters, and usage examples.
